"use strict";

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // amenities, images, tags zaten mevcut. Eklemeye gerek yok.
  },
  down: async (queryInterface, Sequelize) => {
    // amenities, images, tags kaldırılmayacak.
  }
};
